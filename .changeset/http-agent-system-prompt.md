---
"@adapttable/ai": minor
---

Export `agentSystemPrompt` from `@adapttable/ai/http` so a backend can
ship the default AdaptTable HTTP agent rules from this request's catalog
and columns, instead of copying the skill text.
