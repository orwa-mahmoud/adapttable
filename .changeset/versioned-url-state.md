---
"@adapttable/core": minor
---

Version each table's URL state without breaking existing shared links. Unknown
versions, malformed values, duplicate parameters, oversized state, and
multi-table namespaces now follow an explicit recovery policy.
