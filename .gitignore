# Isolated Nightshift / agent worktrees nested under this clone. Never committed.
/worktrees/

# Private working context (plans, notes, AI/skill/tool output). Never committed.
ai_docs/

# Local-only reference notes or scratch material. Never committed or published.
reference/

node_modules/
dist/
build/
coverage/
.turbo/
/node-support-packs/

# Stryker mutation testing — generated reports and scratch.
.stryker-tmp/
reports/mutation/
**/reports/mutation/

# SonarQube / sonar-scanner local output. Never committed.
.scannerwork/
.sonar/

*.log
.DS_Store
.env
.env.*

# tsup transient bundled config
*.bundled_*.mjs

# Playwright E2E — generated reports/results. Never committed.
/test-results/
/playwright-report/
/blob-report/

# Playwright MCP scratch output (screenshots, snapshots) — never committed.
.playwright-mcp/
pg-*.png
# Any scratch image dropped at the repo root (real assets live in site/ and
# apps/*/public/) — agent/browser screenshots must never reach a commit.
/*.png
/*.jpeg
/*.jpg

# scripts/archive-docs-version.mjs scratch site
.archive-work-*/

# Redirect-only site for the previous address, written by scripts/build-redirects.mjs
legacy-site/
