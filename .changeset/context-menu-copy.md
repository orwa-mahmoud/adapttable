---
"@adapttable/react": patch
"@adapttable/antd": patch
---

The context menu's Copy copies the right-clicked cell when `cellNavigation()` is not composed. The Ant Design context menu opens at the pointer.
