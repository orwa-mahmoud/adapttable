---
"@adapttable/core": patch
---

Sorting a column that mixes types has one consistent order: values of different types order booleans, then numbers, then strings, and values of one type compare as before.
