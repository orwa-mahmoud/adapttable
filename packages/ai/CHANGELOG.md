# @adapttable/ai

## 0.1.0

### Minor Changes

- Adaptive table capability discovery: a versioned manifest, a stable
  catalog, lazy `describe` schemas, and `execute` with revision and
  idempotency checks. `tableAgent` is an opt-in React feature on
  `@adapttable/ai/react`.
