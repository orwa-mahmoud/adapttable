---
"@adapttable/core": minor
---

Public plugin API: `TableFeature.setup(host)` registers filter types, editors, aggregators, exporters, column-menu actions, panels, commands and context-menu items on the same surface as the built-in factories.
