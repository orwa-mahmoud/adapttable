---
"@adapttable/react": patch
"@adapttable/antd": patch
---

A selectable row's `aria-selected` follows its checkbox, so screen readers hear a row become selected. Ant Design rows also carry `data-index` and `aria-selected`, as every other kit's rows do.
