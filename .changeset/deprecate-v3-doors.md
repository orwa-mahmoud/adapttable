---
"@adapttable/core": patch
"@adapttable/mui": patch
---

Deprecate adapter-machinery re-exports on `@adapttable/core` (import from `/adapter`), `FilterTypeRegistry.register` / `extend`, and the mui `size` prop. All still work until v3.
