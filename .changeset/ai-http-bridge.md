---
"@adapttable/ai": minor
"@adapttable/core": patch
---

Optional `@adapttable/ai/http` posts a compact capability manifest to a
developer-owned endpoint and executes returned actions through the existing
session. OpenAI tool names are provider-safe, advertised no-ops fail closed,
and live query extras plus a dropped controlled column name stay consistent.
