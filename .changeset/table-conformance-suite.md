---
"@adapttable/core": minor
---

`tableConformanceTests` is a framework-agnostic conformance suite: it runs against any binding's rendered table through a `mount(scenario)` driver and DOM Testing Library, asserting roles, accessible names, part names, row identity, sorting, RTL, the phone layout, the empty state and row selection.
