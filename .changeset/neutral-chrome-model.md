---
"@adapttable/core": minor
"@adapttable/react": patch
---

Core exports the Chrome model: the pure derivation from table state to layout, rows, `data-adapttable-part` names and prop getters, generic over the binding's render node. React's shell and chrome render from it.
