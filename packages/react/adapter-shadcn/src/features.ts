/**
 * shadcn/ui uses the unstyled adapter's native-control feature surface.
 *
 * The API contract validates this canonical forwarding source so the
 * declarative manifest cannot drift to another package.
 *
 * @packageDocumentation
 */
export * from "@adapttable/unstyled/features";
