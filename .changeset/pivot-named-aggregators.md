---
"@adapttable/core": patch
---

A pivot measure can name a plugin-registered aggregator. The table passes its registrations into `pivot()`; a standalone call is unchanged. The URL and Saved Views carry the name the same way they carry `sum`.
