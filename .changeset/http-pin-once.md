---
"@adapttable/ai": minor
---

Pin the table catalog on HTTP `hello` / `schema` so later turns can send
the question without re-attaching the snapshot. Feature changes re-pin.
A host may still send the compact catalog every time.
