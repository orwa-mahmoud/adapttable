---
"@adapttable/react": patch
"@adapttable/mantine": patch
"@adapttable/mui": patch
"@adapttable/chakra": patch
"@adapttable/antd": patch
"@adapttable/radix": patch
"@adapttable/base-ui": patch
"@adapttable/unstyled": patch
---

The grouping strip's remove target spans a row of its own while a chip is in
the air, dashed and in the kit's danger colour, instead of being squeezed in
beside the selects where a reader never found it. It opens on the line below
the chips, so the strip holds every chip exactly where it was when the drag
began. The aggregation column and its function sit side by side as the one
pair they are.
