---
"@adapttable/i18n": minor
---

Add Polish. `pl` joins the bundled presets, so `getLabels("pl")` and
`locales.pl` return Polish chrome strings and `hasLocale("pl")` is true.
The package now ships 18 languages.
